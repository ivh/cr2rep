
echo $SOF_DATA
/home/yjung/TEST/SOF_DATA/cr2re/large/input_data/head

ls $SOF_DATA
dynamic  raw  static

# Run Localy
cd cr2rep
./bootstrap; ./configure; make
cd regtest/tests/
make build_mk
make regcheck

# Yaml files
cr2rep/cr2re_reg/*.yaml

# Jenkins
Set
    "has_regression_tests = true"
in
    P_tools/ci-jenkins/jenkinsfiles/vlt-elt-pipelines/cr2re/cr2re-head.groovy
and commit.

=> Jenkins > VLT/ELT Pipelines > CRIRES+ > cr2re-head > Build w. Param.
    -> Tick run_regression_stage and keep_regression_results

Data have to be exported to pipedata as buildacc:
scp -r /home/yjung/TEST/SOF_DATA/cr2re buildacc@psdlin2:/pipedata/regression/.





