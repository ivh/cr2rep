default_parameters:
  is_default: yes

qc0_parameters:
  is_default: no

science_parameters:
  is_default: no
  packaging_parameters:
    science_fringe:
      - $DATASET/final_results/$TIMESTAMP/$pro.catg$_$arcfile$: [ .* ]
